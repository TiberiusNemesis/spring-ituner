package com.solo.learning.tdourado;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main SpringApplication class. Initiates the ApplicationContext and sets up the Spring Container.
 *
 * @author tiberiusdourado
 */
@SpringBootApplication
public class SpringITunerApplication {
    public static void main(String[] args) {
        SpringApplication.run(SpringITunerApplication.class, args);
    }
}